# 项目概述

本项目参考sylar服务器框架进行改编和简化。通过引入协程、调度器、定时器等模块，利用HOOK技术将Linux系统中传统的同步函数（如'sleep'、'read'等）转化为异步版本。
在实现了异步执行的前提下保留了同步IO的方式。

## 运行环境

Ubuntu 24.10
C++17

## 编译指令

进入文件所在目录
```shell
cd code
```

在code文件下编译链接可执行文件
```shell
g++ -std=c++17 *.cpp -o main -ldl -lpthread
```

运行可执行文件
```shell
./main
```

epoll文件下
```shell
g++ *.cpp -o epoll
```

libevent文件下
```shell
g++ *.cpp -o echoServer -levent
```

### 测试工具安装使用：

```shell
sudo apt update
sudo apt install apache2-utils
```

判断是否安装成功
```shell
ab -V
```

### 通过测试工具测试：

在启动项目之后，测试命令：

```shell
ab -n 100 -c 10 http://127.0.0.1:8080/
```

* -n 连接数目
* -c 并发数目

## 主要模块介绍

### fiber
* 使用非对称的独立栈协程
* 支持调度器和任务协程之间切换

### scheduler
* 使用线程池和任务队列维护任务
* 工作线程采用FIFO运行协程，负责将epoll中就绪文件描述符事件和超时任务加入队列

### timer
* 采用小根堆管理定时器

## 核心技术

* 线程同步与互斥
* 线程池
* epoll事件模型
* Linux网络编程
* 同步与异步IO
* HOOK技术

## 待优化和扩展

### 性能调优
经测试当前性能并没有强于epoll和libevent，经分析可能是fiber频繁创建销毁导致，需要调整提高性能

### 内存池优化
当前协程创建时自动分配独立栈空间，可通过内存池技术优化提高内存利用率

### 调度算法
协程的调度算法仅仅采用了FIFO，可以尝试引进优先级、时间片等，满足更多场景需求